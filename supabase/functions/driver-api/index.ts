import { createClient } from "npm:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization,apikey,content-type,x-driver-session"};
const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,"Content-Type":"application/json"}});
const admin=()=>createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
async function hashToken(token:string){const buf=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(token));return [...new Uint8Array(buf)].map(x=>x.toString(16).padStart(2,"0")).join("");}
async function driverId(session:string){const h=await hashToken(session),db=admin();const {data,error}=await db.from("driver_sessions").select("driver_id,expires_at,driver_accounts!inner(id,driver_name,active)").eq("token_hash",h).gt("expires_at",new Date().toISOString()).maybeSingle();if(error||!data||!data.driver_accounts?.active)return null;await db.from("driver_sessions").update({last_seen_at:new Date().toISOString()}).eq("token_hash",h);return{id:data.driver_id,name:data.driver_accounts.driver_name};}
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const body=await req.json(),db=admin();if(body.action==="login"){const {data,error}=await db.rpc("driver_login",{p_login_name:String(body.login_name||""),p_pin:String(body.pin||"")});if(error)return json({ok:false,message:error.message},400);return json(data);}const session=String(body.session_token||req.headers.get("x-driver-session")||""),driver=await driverId(session);if(!driver)return json({ok:false,message:"Session expired"},401);if(body.action==="dashboard"){const {data,error}=await db.rpc("get_driver_dashboard",{p_session_token:session});if(error)return json({ok:false,message:error.message},400);return json(data);}if(body.action==="upload_url"){const outletId=String(body.outlet_id||""),orderId=String(body.order_id||"");const {data:o}=await db.from("outlets").select("id,store_name,status,driver,delivery_charge").eq("id",outletId).eq("order_id",orderId).eq("driver",driver.name).maybeSingle();if(!o)return json({ok:false,message:"Outlet is not assigned to this driver"},403);if(o.status!=="completed")return json({ok:false,message:"Outlet packing is not completed yet"},400);const ext=String(body.extension||"jpg").toLowerCase().replace(/[^a-z0-9]/g,"")||"jpg",path=orderId+"/"+outletId+"/"+crypto.randomUUID()+"."+ext;const {data,error}=await db.storage.from("delivery-invoices").createSignedUploadUrl(path);if(error)return json({ok:false,message:error.message},400);await db.from("delivery_records").upsert({order_id:orderId,outlet_id:outletId,driver_id:driver.id,invoice_path:path,invoice_filename:String(body.filename||"invoice"),invoice_mime_type:String(body.mime_type||"image/jpeg"),ocr_status:"pending",status:"pending",delivery_charge:Number(o.delivery_charge||0),updated_at:new Date().toISOString()},{onConflict:"order_id,outlet_id"});return json({ok:true,path,token:data.token});}if(body.action==="rejection_photo_url"){const outletId=String(body.outlet_id||""),orderId=String(body.order_id||""),itemId=String(body.item_id||"");const {data:o}=await db.from("outlets").select("id,driver").eq("id",outletId).eq("order_id",orderId).eq("driver",driver.name).maybeSingle();if(!o)return json({ok:false,message:"Outlet is not assigned to this driver"},403);const {data:i}=await db.from("order_items").select("id,status").eq("id",itemId).eq("outlet_id",outletId).eq("order_id",orderId).maybeSingle();if(!i||!["MISSING","PARTIAL"].includes(String(i.status||"").toUpperCase()))return json({ok:false,message:"Photo can only be added for a missing or partial item"},400);const ext=String(body.extension||"jpg").toLowerCase().replace(/[^a-z0-9]/g,"")||"jpg";const path=orderId+"/"+outletId+"/rejected/"+itemId+"/"+crypto.randomUUID()+"."+ext;const {data,error}=await db.storage.from("delivery-evidence").createSignedUploadUrl(path);if(error)return json({ok:false,message:error.message},400);return json({ok:true,path,token:data.token});}
if(body.action==="save_rejection_photo"){const outletId=String(body.outlet_id||""),orderId=String(body.order_id||""),itemId=String(body.item_id||""),path=String(body.path||"");const {data:o}=await db.from("outlets").select("id,driver").eq("id",outletId).eq("order_id",orderId).eq("driver",driver.name).maybeSingle();if(!o)return json({ok:false,message:"Outlet is not assigned to this driver"},403);const {data:i}=await db.from("order_items").select("id,status,product_name").eq("id",itemId).eq("outlet_id",outletId).eq("order_id",orderId).maybeSingle();if(!i||!["MISSING","PARTIAL"].includes(String(i.status||"").toUpperCase()))return json({ok:false,message:"Invalid rejected item"},400);if(!path.startsWith(orderId+"/"+outletId+"/rejected/"+itemId+"/"))return json({ok:false,message:"Invalid photo path"},400);const {data:rec}=await db.from("delivery_records").select("rejection_photos").eq("order_id",orderId).eq("outlet_id",outletId).eq("driver_id",driver.id).maybeSingle();const photos=Array.isArray(rec?.rejection_photos)?rec.rejection_photos:[];photos.push({item_id:itemId,product_name:i.product_name,path,filename:String(body.filename||"rejected-item"),mime_type:String(body.mime_type||"image/jpeg"),uploaded_at:new Date().toISOString()});const {error}=await db.from("delivery_records").upsert({order_id:orderId,outlet_id:outletId,driver_id:driver.id,rejection_photos:photos,updated_at:new Date().toISOString()},{onConflict:"order_id,outlet_id"});if(error)return json({ok:false,message:error.message},400);return json({ok:true});}
if(body.action==="mark_delivered"){const outletId=String(body.outlet_id||""),orderId=String(body.order_id||"");const {data:o}=await db.from("outlets").select("id,status,driver").eq("id",outletId).eq("order_id",orderId).eq("driver",driver.name).maybeSingle();if(!o)return json({ok:false,message:"Outlet is not assigned to this driver"},403);if(o.status!=="completed")return json({ok:false,message:"Outlet packing is not completed"},400);const {data:r}=await db.from("delivery_records").select("invoice_path").eq("order_id",orderId).eq("outlet_id",outletId).eq("driver_id",driver.id).maybeSingle();if(!r?.invoice_path)return json({ok:false,message:"Upload invoice image before marking delivered"},400);const {data:file,error:fileError}=await db.storage.from("delivery-invoices").download(r.invoice_path);if(fileError||!file)return json({ok:false,message:"Invoice upload could not be verified"},400);const {error}=await db.from("delivery_records").update({status:"delivered",delivered_at:new Date().toISOString(),invoice_uploaded_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("order_id",orderId).eq("outlet_id",outletId).eq("driver_id",driver.id);if(error)return json({ok:false,message:error.message},400);return json({ok:true});}return json({ok:false,message:"Unknown action"},400);}catch(e){return json({ok:false,message:String(e?.message||e)},500)}});